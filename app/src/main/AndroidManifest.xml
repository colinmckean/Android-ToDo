<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codeclan.example.todo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListAllListsActivity"
            android:label="@string/title_activity_list_all_lists"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ListOfToDoItems"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ViewSingleToDo"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".NewListActivity"
            android:parentActivityName=".Lists" />
        <activity
            android:name=".NewToDoActivity"
            android:parentActivityName=".Lists" />
        <activity
            android:name=".EditToDoActivity"
            android:parentActivityName=".Lists" />
        <activity
            android:name=".Lists"
            android:label="@string/title_activity_lists"
            android:parentActivityName=".Lists" />
        <activity
            android:name=".ListToDoItems"
            android:label="@string/title_activity_list_to_do_items"
    android:parentActivityName=".MainActivity"/>
    </application>

</manifest>